USE ROLE SYSADMIN;
USE DATABASE SALES;

--
SELECT 
    *
FROM
    BRONZE.ERP_PX_CAT_G1V2;

-- 1. Check ID
SELECT 
    ID,
    COUNT(1)
FROM
    BRONZE.ERP_PX_CAT_G1V2
GROUP BY
    ID
HAVING 
    COUNT(1)>1;

-- 2. Check Category

SELECT 
    DISTINCT CAT, SUBCAT
FROM
    BRONZE.ERP_PX_CAT_G1V2
ORDER BY CAT, SUBCAT;
